#!/bin/bash

docker-compose up -d

NGROK=$(docker-compose port tunnel 4040)
PUBLIC_URL=$(curl -s $NGROK/api/tunnels | docker run -i --rm kamermans/jq '.tunnels[]|select(.public_url|startswith("https")).public_url')

echo 'Webhook available at:'
echo $PUBLIC_URL